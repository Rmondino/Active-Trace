## MODIFIED Requirements

### Requirement: Sidebar adaptativo por permisos

El sidebar SHALL incluir los siguientes ítems adicionales:
- **Mi Perfil** (`/perfil`) — todos los usuarios autenticados
- **Mensajes** (`/inbox`) — todos los usuarios autenticados
